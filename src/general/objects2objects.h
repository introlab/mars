#ifndef __MARS_GENERAL_OBJECTS2OBJECTS
#define __MARS_GENERAL_OBJECTS2OBJECTS

    #include "objects.h"
    #include "profiler.h"
    #include <time.h>

    int objects2objects_process(objects * objs, profiler_obj * profiler);

#endif